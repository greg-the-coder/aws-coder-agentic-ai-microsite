# Agentic AI Observability Dashboard: Recommendations

## Context

This document proposes a CloudWatch-based observability dashboard that demonstrates how the AWS + Coder microsite (https://partner.coderdemo.io/aws+coder) was developed and published using Coder Agents in governed workspaces. It synthesizes activity across four observable dimensions — Coder Agent execution, LLM interactions, GitHub operations, and AWS infrastructure — into a single pane of glass using Amazon CloudWatch Generative AI Observability.

The dashboard serves as both a **proof-of-concept** for the microsite's "Modernize" and "Multiply" messaging and a **reference implementation** for customers evaluating governed agentic development.

---

## Dashboard Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                    CloudWatch GenAI Observability                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌────────────┐ │
│  │ Coder Agent │  │ LLM / Model │  │   GitHub    │  │    AWS     │ │
│  │  Activity   │  │  Activity   │  │  Activity   │  │   Infra    │ │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └─────┬──────┘ │
│         │                 │                 │               │         │
│         ▼                 ▼                 ▼               ▼         │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │              Embedded Metric Format (EMF) Logs                   │ │
│  │              + OpenTelemetry Traces (X-Ray)                      │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│         │                 │                 │               │         │
│         ▼                 ▼                 ▼               ▼         │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                 CloudWatch Custom Dashboard                      │ │
│  │        + Bedrock Guardrails Alerts + Anomaly Detection           │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Panel 1: Coder Agent Activity

**Data Source:** Coder workspace telemetry + CloudWatch Agent (EMF logs)
**Namespace:** `CoderAgentMetrics`

### Metrics

| Metric | Description | This Session's Value |
|--------|-------------|---------------------|
| `AgentSessionDuration` | Total wall-clock time of agent session | ~90 minutes |
| `AgentTaskCount` | Number of discrete tasks executed | 12+ (research, write, deploy, iterate) |
| `WorkspaceUptime` | Time workspace was active | Full session duration |
| `FileOperationsCount` | Files created/modified/deleted | 6 files (HTML, CSS, PNG, README, LICENSE, refarch) |
| `ShellCommandsExecuted` | CLI commands run in workspace | ~120 (aws cli, git, curl, python) |
| `S3OperationsCount` | S3 put/copy/sync operations | 28+ uploads |
| `CloudFrontInvalidations` | Cache invalidation requests | 12 |
| `DeploymentCount` | Successful site deployments | 8 iterative deployments |

### Dashboard Widgets

1. **Agent Task Timeline** — Gantt-style view of task execution over time
2. **Commands/Minute** — Line chart showing agent activity intensity
3. **Deployment Frequency** — Bar chart of S3 uploads + CF invalidations
4. **Files Changed per Commit** — Stacked bar (insertions/deletions)

---

## Panel 2: LLM / Model Activity

**Data Source:** Bedrock model invocation logs + Strands Agent traces
**Namespace:** `StrandsAgentMetrics` / Bedrock native metrics

### Metrics

| Metric | Description | Estimated Value |
|--------|-------------|-----------------|
| `InputTokens` | Tokens sent to model per invocation | ~800K total input |
| `OutputTokens` | Tokens generated by model | ~200K total output |
| `TotalTokens` | Combined token consumption | ~1M tokens |
| `InvocationCount` | Number of LLM API calls | ~50–80 calls |
| `InvocationLatency` | Time per model response (p50/p95/p99) | 2–15s range |
| `ToolCallCount` | Tools invoked by agent (file_write, execute, etc.) | ~150 |
| `ReasoningCycles` | Agent think→act→observe loops | ~40 cycles |
| `ModelId` | Which model was used | Claude Sonnet 4 |
| `EstimatedCost` | Token cost at Bedrock rates | ~$8–15 per session |

### Dashboard Widgets

1. **Token Usage Over Time** — Area chart (input vs output tokens per minute)
2. **Invocation Latency (p50/p95)** — Line chart with SLA threshold
3. **Cost Accumulation** — Running total of estimated spend
4. **Tool Usage Breakdown** — Pie chart (execute, write_file, read_file, edit_files, spawn_agent)
5. **Reasoning Cycles per Task** — Bar chart showing complexity distribution

### Sample Logs Insights Query

```sql
fields @timestamp, attributes.gen_ai.prompt, attributes.gen_ai.usage.total_tokens, attributes.user.id
| filter attributes.gen_ai.usage.total_tokens > 5000
| sort attributes.gen_ai.usage.total_tokens desc
| limit 20
```

---

## Panel 3: GitHub Activity

**Data Source:** GitHub webhooks → EventBridge → CloudWatch Logs
**Namespace:** `GitHubActivityMetrics`

### Metrics

| Metric | Description | This Session's Value |
|--------|-------------|---------------------|
| `CommitCount` | Total commits pushed | 8 |
| `LinesAdded` | Insertions across all commits | 2,424 |
| `LinesRemoved` | Deletions across all commits | 29 |
| `FilesChanged` | Unique files modified | 6 |
| `PushEvents` | Git push operations | 8 |
| `ForcePushCount` | Force pushes (initial setup) | 1 |
| `CommitFrequency` | Commits per hour | ~7/hr |
| `AvgCommitSize` | Average lines changed per commit | ~300 lines |

### Dashboard Widgets

1. **Commit Timeline** — Scatter plot of commits over session duration
2. **Code Volume** — Stacked area (lines added vs removed)
3. **Commit Messages** — Log table with timestamps and descriptions
4. **Repository Growth** — Line chart of cumulative file count

---

## Panel 4: AWS Infrastructure

**Data Source:** CloudWatch native metrics + CloudTrail
**Namespace:** Standard AWS service namespaces

### Metrics

| Metric | Service | Description |
|--------|---------|-------------|
| `BucketSizeBytes` | S3 | Size of `aws-coder-microsites-coderdemo` |
| `NumberOfObjects` | S3 | Object count in bucket |
| `Requests` | CloudFront | Total requests to `/aws+coder*` |
| `BytesDownloaded` | CloudFront | Bandwidth served |
| `4xxErrorRate` | CloudFront | Client error percentage |
| `FunctionInvocations` | CloudFront Functions | URL rewrite function calls |
| `DistributionDeployments` | CloudFront | Config update count |
| `APICallCount` | CloudTrail | Total AWS API calls by agent |

### Dashboard Widgets

1. **CloudFront Request Rate** — Line chart of hits over time
2. **S3 Storage Growth** — Step chart showing object additions
3. **API Calls by Service** — Stacked bar (S3, CloudFront, IAM, STS)
4. **Error Rate** — Single-value gauge with threshold alert
5. **Infrastructure Cost** — Running estimate (CF + S3 + data transfer)

---

## Bedrock Guardrails Recommendations

Based on the observed agent behavior in this session, the following Bedrock Guardrails should be deployed to limit and alert on erroneous or malicious activity:

### 1. Content Filters (Guardrail Policy)

| Filter | Threshold | Rationale |
|--------|-----------|-----------|
| **Denied Topics** | Block: PII generation, credential exposure | Agent should never generate or expose secrets, API keys, or PII in published content |
| **Word Filters** | Alert: internal hostnames, private IPs, account IDs | Prevent accidental exposure of infrastructure details in public-facing output |
| **Sensitive Information** | Block: SSN, credit card, AWS access keys | Redact any sensitive patterns before content reaches S3 |

### 2. Token & Cost Guardrails

| Guardrail | Configuration | Alert Action |
|-----------|---------------|--------------|
| **Max tokens per invocation** | 32,000 output tokens | CloudWatch Alarm → SNS notification |
| **Max tokens per session** | 2,000,000 total | Hard stop + alert to workspace owner |
| **Cost ceiling per session** | $25 estimated spend | Throttle to smaller model + alert |
| **Anomaly detection on token rate** | 3σ above baseline | Auto-pause agent + Slack notification |

### 3. Tool Execution Guardrails

| Guardrail | Policy | Enforcement |
|-----------|--------|-------------|
| **Allowed AWS services** | S3, CloudFront, CloudWatch only | Agent Firewall network policy (deny all except allowlisted endpoints) |
| **Blocked commands** | `rm -rf /`, `aws iam`, `aws ec2 terminate`, destructive operations | Agent Firewall command blocklist |
| **Git push targets** | Only `github.com/greg-the-coder/*` | Network policy restricting git remote URLs |
| **File write scope** | Only `/home/coder/microsite/**` | Workspace-level filesystem isolation |
| **S3 bucket scope** | Only `aws-coder-microsites-coderdemo` | IAM policy condition on resource ARN |

### 4. Output Validation Guardrails

| Guardrail | Check | Action on Failure |
|-----------|-------|-------------------|
| **HTML validation** | Published HTML must be well-formed | Block S3 upload, alert |
| **Content size limit** | No single file > 500KB | Block upload, alert |
| **No external scripts** | HTML must not reference external JS (XSS prevention) | Block upload, alert |
| **Brand compliance** | Scan for competitor mentions, off-brand language | Alert for human review |
| **Link validation** | All href targets must resolve (no broken links) | Warning alert |

### 5. Behavioral Anomaly Detection

| Signal | Baseline (this session) | Alert Threshold |
|--------|------------------------|-----------------|
| Commands per minute | ~2–3 | > 20/min (possible runaway loop) |
| S3 uploads per hour | ~4 | > 30/hr (possible infinite deploy loop) |
| Git force pushes | 1 (initial only) | > 1 after initial setup |
| CloudFront invalidations/hr | ~3 | > 15/hr (cost concern + possible abuse) |
| Unique AWS services accessed | 3 (S3, CF, STS) | > 6 (scope creep alert) |
| Failed API calls | < 5% | > 20% (possible permission escalation attempts) |

---

## Implementation Path

### Phase 1: Instrumentation (1–2 days)

1. **Install CloudWatch Agent** on Coder workspace template with OpenTelemetry collector
2. **Enable Bedrock model invocation logging** to CloudWatch Logs
3. **Configure GitHub webhook** → EventBridge → CloudWatch Logs
4. **Emit custom EMF metrics** from Coder Agent wrapper script for tool usage tracking

### Phase 2: Dashboard (1 day)

1. Create CloudWatch Dashboard with 4 panel groups (Agent, LLM, GitHub, Infra)
2. Import prebuilt Bedrock GenAI observability widgets from CloudWatch console
3. Add Logs Insights query widgets for top-N token consumers and cost breakdowns
4. Configure X-Ray trace groups for agent session correlation

### Phase 3: Guardrails & Alerting (1–2 days)

1. Create Bedrock Guardrail with content filters + denied topics
2. Deploy Agent Firewall policies in Coder workspace template
3. Create CloudWatch Alarms for token ceiling, cost anomaly, and behavioral baselines
4. Configure SNS topic → Slack/email for alert routing
5. Add CloudWatch Anomaly Detection on `TotalTokens` and `ShellCommandsExecuted`

### Phase 4: Iterate

1. Correlate dashboard signals with deployment success/failure outcomes
2. Tune guardrail thresholds based on observed agent patterns
3. Add CloudWatch Synthetics canary to validate published microsite health
4. Integrate with Coder's AI Gateway audit log for unified LLM usage view

---

## Cost Estimate (Dashboard Infrastructure)

| Component | Monthly Cost |
|-----------|-------------|
| CloudWatch Dashboards (1 custom) | $3 |
| CloudWatch Logs (ingestion ~5GB/mo) | $2.50 |
| CloudWatch Metrics (50 custom metrics) | $15 |
| X-Ray traces (~10K traces/mo) | $5 |
| CloudWatch Alarms (10 alarms) | $1 |
| EventBridge rules (GitHub webhook) | < $1 |
| **Total** | **~$27/month** |

---

## Summary

This dashboard demonstrates that the development of a co-branded microsite — from research through iterative refinement and deployment — is fully observable using AWS-native tooling. The combination of:

- **CloudWatch GenAI Observability** for LLM tracing and token economics
- **Coder Agent Firewall** for execution-layer governance
- **Bedrock Guardrails** for content and cost safety rails
- **CloudTrail + X-Ray** for audit and dependency mapping

...provides the enterprise control plane necessary to run autonomous AI agents at scale with confidence. This is the "Modernize" and "Multiply" story in practice — not theoretical, but demonstrated through the very creation of this site.

---

## Appendix: Coder-Native OTEL Integration with AWS CloudWatch

Analysis of the [Coder open-source repository](https://github.com/coder/coder/) reveals deep, production-ready OpenTelemetry instrumentation that maps directly to AWS CloudWatch Generative AI Observability.

### Coder's AI Bridge (`aibridge/`) — The Telemetry Source

Coder's **AI Bridge** is an HTTP handler mounted at `/api/v2/aibridge/<provider>/*` that intercepts every AI client request. It supports: Claude Code, Codex, Kiro, Cursor, GitHub Copilot, Windsurf, Coder Agents, and more.

This is the **single integration point** for all agentic AI observability — every LLM interaction in a Coder workspace passes through this bridge.

### Available OTEL Trace Spans

| Span Name | Context | Key Attributes |
|-----------|---------|----------------|
| `Intercept` | Top-level interception of AI request | `provider`, `model`, `user_id`, `streaming`, `aws_bedrock` |
| `Intercept.RecordTokenUsage` | Token consumption per response | `type` (input/output/cache_read/cache_write) |
| `Intercept.RecordPromptUsage` | User prompt capture | `interception_id` |
| `Intercept.RecordToolUsage` | Tool invocations | `mcp_tool_name`, `mcp_server_name` |
| `Intercept.RecordModelThought` | Model reasoning/thinking | reasoning content |
| `Intercept.ProcessRequest.ToolCall` | MCP tool execution | `mcp_input`, `mcp_proxy_name`, `mcp_server_url` |
| `Passthrough` | Non-intercepted forwarded request | `passthrough_url`, `passthrough_method` |
| `StreamableHTTPServerProxy.Init` | MCP server initialization | `mcp_tool_count` |

### Available Prometheus Metrics

| Metric | Type | Labels |
|--------|------|--------|
| `interceptions_total` | Counter | provider, model, status, route, method, initiator_id, client |
| `interceptions_inflight` | Gauge | provider, model, route |
| `interceptions_duration_seconds` | Histogram | provider, model (buckets: 0.5-120s) |
| `tokens_total` | Counter | provider, model, type (input/output/cache_read/cache_write), initiator_id, client |
| `prompts_total` | Counter | provider, model, initiator_id, client |
| `injected_tool_invocations_total` | Counter | provider, model, server, name |
| `non_injected_tool_selections_total` | Counter | provider, model, name |
| `circuit_breaker_state` | Gauge | provider, endpoint, model |
| `circuit_breaker_trips_total` | Counter | provider, endpoint, model |

### Trace Attributes for AWS Bedrock Detection

Coder natively tracks an `aws_bedrock` boolean attribute on every intercepted request, enabling CloudWatch to filter and group Bedrock-specific traffic separately from other providers.

### Database-Persisted Audit Trail

Beyond OTEL export, the AI Bridge records to persistent database tables:

| Table | Data |
|-------|------|
| `aibridge_interceptions` | Provider, model, initiator, timestamps, client, session |
| `aibridge_token_usages` | Input/output/cache tokens per response |
| `aibridge_user_prompts` | Full user prompt text |
| `aibridge_tool_usages` | Tool name, server URL, arguments, injected flag |
| `aibridge_model_thoughts` | Reasoning, thinking, commentary content |

---

### Integration Architecture: Coder to AWS CloudWatch

```
Coder Platform (EKS)
|
|-- AI Bridge (aibridge/)
|     |-- OTLP/gRPC --> ADOT Collector (sidecar)
|     |                    |-- Traces --> X-Ray
|     |                    |-- Metrics --> CloudWatch
|     |
|     |-- Prometheus /metrics --> CloudWatch Agent
|     |                             |-- EMF logs
|     |                             |-- Custom namespace: CoderAIBridge
|
AWS CloudWatch GenAI Observability
|-- X-Ray Traces (Agent spans, tool calls, trace map)
|-- Custom Metrics (tokens_total, latency p95, error rate)
|-- Logs Insights (prompt search, cost queries, anomaly logs)
|-- GenAI Dashboard (prebuilt: token usage, latency, tool breakdown)
```

### Mapping: Coder Attributes to GenAI Semantic Conventions

| Coder AI Bridge Attribute | OpenTelemetry GenAI Convention | CloudWatch Display |
|---------------------------|-------------------------------|-------------------|
| `provider` | `gen_ai.system` | Provider filter |
| `model` | `gen_ai.request.model` | Model breakdown |
| `tokens_total{type="input"}` | `gen_ai.usage.input_tokens` | Input token chart |
| `tokens_total{type="output"}` | `gen_ai.usage.output_tokens` | Output token chart |
| `interceptions_duration_seconds` | `gen_ai.client.operation.duration` | Latency panel |
| `mcp_tool_name` | `gen_ai.tool.name` | Tool usage breakdown |
| `user_id` / `initiator_id` | `gen_ai.user.id` | Per-user analytics |
| `aws_bedrock` | Custom dimension | Bedrock-specific views |
| `streaming` | `gen_ai.request.streaming` | Streaming vs sync |

### Configuration (Single Environment Variable)

Coder's existing OTLP exporter (`coderd/tracing/exporter.go`) requires only:

```bash
# Point Coder at the ADOT Collector
export OTEL_EXPORTER_OTLP_ENDPOINT="http://adot-collector:4317"
```

All AI Bridge spans and the full coderd trace context will flow to CloudWatch automatically. No code changes required.

### Prometheus Scrape for CloudWatch Metrics

The CloudWatch Agent can scrape Coder's Prometheus `/metrics` endpoint:

```yaml
# CloudWatch Agent config excerpt
metrics:
  metrics_collected:
    prometheus:
      prometheus_config_path: /etc/prometheus/coder-scrape.yml
      emf_processor:
        metric_namespace: CoderAIBridge
        metric_declaration:
          - source_labels: [__name__]
            label_matcher: "^(interceptions_total|tokens_total|prompts_total|injected_tool_invocations_total|circuit_breaker_.*)$"
            dimensions: [[provider, model], [provider, model, initiator_id]]
```

---

### Opportunities: What This Enables

With Coder's native telemetry flowing to CloudWatch, the dashboard proposed in this document becomes **zero-custom-code**:

| Dashboard Panel | Coder Telemetry Source | Custom Code Needed |
|----------------|----------------------|-------------------|
| LLM Token Usage | `tokens_total` metric | None - direct scrape |
| Invocation Latency | `interceptions_duration_seconds` | None - direct scrape |
| Tool Usage | `injected_tool_invocations_total` | None - direct scrape |
| Per-User Cost | `tokens_total` x Bedrock pricing | Metric math only |
| Agent Activity | `Intercept` spans via X-Ray | None - OTLP export |
| MCP Server Health | `circuit_breaker_state` | None - direct scrape |
| Prompt Audit | `aibridge_user_prompts` DB table | Logs Insights query |
| Model Reasoning | `aibridge_model_thoughts` DB table | Logs Insights query |
| Bedrock-Specific | Filter on `aws_bedrock=true` | Trace attribute filter |

**Key insight: Coder already emits everything needed.** The integration is purely configuration — set the OTLP endpoint and CloudWatch Agent scrape target. No additional instrumentation libraries, no custom metric emission code, no agent modifications required.

### Supported AI Clients (all telemetry-emitting)

- Claude Code
- Kiro
- Cursor
- Windsurf
- GitHub Copilot (VS Code + CLI)
- Codex (OpenAI)
- Coder Agents (autonomous)
- Roo Code
- Zed

Every interaction from any of these clients through a Coder workspace is captured, traced, metered, and auditable via the same CloudWatch GenAI Observability dashboard.
